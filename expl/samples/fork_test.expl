int main()
{
 decl
	int temp, pid;
	str fname;
 enddecl
 begin
	breakpoint;
	pid = exposcall("Fork");
	breakpoint;
	if (pid == 0) then
		temp = exposcall("Exec", "odd.xsm");
	else
		temp = exposcall("Exec", "even.xsm");
	endif;

 	return 0;
 end
}

